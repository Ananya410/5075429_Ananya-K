#include <stdio.h>
int main() {
    int total;
    if (scanf("%d", &total) !=1 || total <1) {
        return 0;
    }
    int xorValue = 0;
    for(int k = 0; k < total; ++k) {
    int current;
        if(scanf("%d", &current) != 1) {
            return 0;
        }
        xorValue ^= current;
    }
     printf("%d\n", xorValue);
    return 0;
}
